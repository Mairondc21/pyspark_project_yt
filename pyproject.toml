[project]
name = "yt-005"
version = "0.1.0"
description = ""
authors = [
    {name = "Mairon_L"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyspark (==3.5.1)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
